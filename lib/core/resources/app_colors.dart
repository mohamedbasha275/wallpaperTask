import 'package:flutter/material.dart';

class AppColors {
  static const MaterialColor primarySwatch = Colors.indigo;
  static const Color primary = Color.fromRGBO(89, 68, 255, 1);
  static Color splash = Colors.deepPurpleAccent;
  static Color secondary = const Color.fromRGBO(255, 85, 0, 1);
  static Color reset = Colors.redAccent;
  static Color failure = Colors.red;
  static Color success = Colors.green;
  static Color cancel = Colors.lightBlue;
  static Color info = Colors.lightBlue;
  static Color banner = Colors.amberAccent;
  static Color leave = Colors.deepOrangeAccent;
  static Color expansion = Colors.deepPurple[50]!;
  static Color white = Colors.white;
  static Color darkGrey = Colors.blueGrey;
  static Color selectedItem = Colors.white;
  static Color unSelectedItem = Colors.white60;
  static Color black = Colors.black;
  static Color grey = Colors.grey;
  static Color transparentColor=Colors.transparent;
}
